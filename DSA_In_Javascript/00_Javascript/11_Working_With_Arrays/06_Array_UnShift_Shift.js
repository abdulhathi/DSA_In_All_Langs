const names = ["Abdul", "Aysha", "Amira"]
console.log(names, names.length)

// Add a value in the first place
const length = names.unshift("Afsar");
console.log(names, length);

// Remove a value from the first place
value = names.shift();
console.log(names, value);