const names = ["Abdul", "Aysha", "Afsar"];
console.log(names, names.length);

// Add the element in the last
names.push("Afraz")
const length = names.push("Amira");
console.log(names, length)

// Remove the element in the last
console.log(names.pop());
console.log(names);